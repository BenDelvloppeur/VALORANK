{
  "name": "valorank",
  "version": "0.1.0",
  "private": true,
  "description": "Plateforme de coaching Valorant — monorepo (Next.js + Express).",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm:dev:backend\" \"npm:dev:frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "create-admin": "npm run create-admin --prefix backend",
    "create-coach-accounts": "npm run create-coach-accounts --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "prisma:studio": "npm run prisma:studio --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
